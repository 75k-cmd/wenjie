#include <iostream>
using namespace std;


int main()
 {
    // 关键字：在C++中有一些被编译器预先定义好的具有特殊意义的单词叫关键字，如：int、return、if、else等
    // 关键字的使用
    int a = 100; // int是关键字，表示定义一个整型变量a，值为100
    cout << "a = " << a << endl; // 输出a的值

    return 0;
}